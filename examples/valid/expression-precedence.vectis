// GHOST FIVE // VECTIS
// Canonical language example: expression precedence.
mission "Expression precedence" {
    confidence 1 + 2 * 3;

    when ready && score >= 0.80 || override {
        publish result;
    }
}
